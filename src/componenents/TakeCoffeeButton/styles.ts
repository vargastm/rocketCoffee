import styled from "styled-components";

export const TakeCoffee = styled.button`
    background: none;
    color: var(--text-color);

    text-transform: uppercase;
    font-weight: 800;
    font-size: 0.6875rem;

    padding: 0.6875rem 2.375rem;

    border: 1px solid var(--button);
    border-radius: 6px;

    cursor: pointer;
    z-index: 1;
`